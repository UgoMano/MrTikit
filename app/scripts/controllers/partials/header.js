'use strict';

/**
 * @ngdoc function
 * @name mrtikitApp.controller:HeaderCtrl
 * @description
 * # AboutCtrl
 * Controller of the mrtikitApp
 */
angular.module('mrtikitApp').controller('HeaderCtrl', function ($scope, $window) {
    console.log($scope.scrollPosition);
    /*$scope.$watch($scrollPosition, function(){
        console.log($scrollPosition);
    });*/
});